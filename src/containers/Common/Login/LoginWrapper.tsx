import styled from '../../../utils/styled-components';
import { Wrapper } from '../../../components/Shared/FullWrapper';

export const LoginWrapper = Wrapper.extend`
    display: flex;
    align-items: center;
    justify-content: center;
`;
