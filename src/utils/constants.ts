export const UndefinedId = 0;

export enum Status {
    Off = 0,
    On = 1,
}

export enum PriceFixed {
    Amount = 1,
    Percentage = 2,
}

export enum Payper {
    Click = 3,
    Conversion = 4,
    ClickConversion = 5,
}

export enum CreativeType {
    Text = 1,
    Picture = 3,
    JS = 5,
}

export enum NoticeType {
    New = 1,
    Restart = 2,
    GrowingUpUnitPrice = 3,
    ConditionsChange = 9,
}

export const DefaultMaxCreativeNum = 1;
